temp = input('What is the temperature?')
if temp[-1] == 'F':
    should = 'C'
    tempInt = int(temp[0:-1])
    tran = ( tempInt - 32 )/1.8
elif temp[-1]=='C':
    should = 'F'
    tempInt = int(temp[0:-1])
    tran = tempInt * 1.8 + 32
print('The converted temperature is %d' % tran + should )
